
import { createActions } from 'redux-actions'

export const SET_SECTION_NAME = 'GET_TABLE'

/* action creators */
const actions = createActions({
  SET_SECTION_NAME: undefined
})

// export const setSectionName = () => async dispatch => {
//   return dispatch(actions.setSectionName(allTables))
// }

export default actions
